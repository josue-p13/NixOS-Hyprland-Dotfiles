# btop theme generated by wallust
# Colors are hex values

# Main background, empty for terminal default, need to be empty if you want transparent background
theme[main_bg]=""

# Main text color
theme[main_fg]="{{foreground}}"

# Title color for boxes
theme[title]="{{foreground}}"

# Higlight color for keyboard shortcuts
theme[hi_fg]="{{color1}}"

# Background color of selected items
theme[selected_bg]="{{color8}}"

# Foreground color of selected items
theme[selected_fg]="{{color15}}"

# Color of inactive/disabled text
theme[inactive_fg]="{{color8}}"

# Color of text appearing on top of graphs, i.e uptime and current network graph scaling
theme[graph_text]="{{color15}}"

# Misc colors for processes box including mini cpu graphs, details memory graph and details status text
theme[proc_misc]="{{color3}}"

# Cpu box outline color
theme[cpu_box]="{{color12}}"

# Memory/disks box outline color
theme[mem_box]="{{color12}}"

# Net up/down box outline color
theme[net_box]="{{color12}}"

# Processes box outline color
theme[proc_box]="{{color12}}"

# Box divider line and small boxes line color
theme[div_line]="{{color12}}"

# Temperature graph colors
theme[temp_start]="{{color4}}"
theme[temp_mid]="{{color5}}"
theme[temp_end]="{{color1}}"

# CPU graph colors
theme[cpu_start]="{{color2}}"
theme[cpu_mid]="{{color3}}"
theme[cpu_end]="{{color1}}"

# Mem/Disk free meter
theme[free_start]="{{color10}}"
theme[free_mid]=""
theme[free_end]="{{color2}}"

# Mem/Disk cached meter
theme[cached_start]="{{color12}}"
theme[cached_mid]=""
theme[cached_end]="{{color4}}"

# Mem/Disk available meter
theme[available_start]="{{color11}}"
theme[available_mid]=""
theme[available_end]="{{color3}}"

# Mem/Disk used meter
theme[used_start]="{{color9}}"
theme[used_mid]=""
theme[used_end]="{{color1}}"

# Download graph colors
theme[download_start]="{{color12}}"
theme[download_mid]=""
theme[download_end]="{{color4}}"

# Upload graph colors
theme[upload_start]="{{color13}}"
theme[upload_mid]=""
theme[upload_end]="{{color5}}"
